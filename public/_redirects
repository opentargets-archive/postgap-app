# Redirect domain aliases to primary domain
# https://postgwas.opentargets.io/* https://genetics.opentargets.io/:splat 301!

/*  /index.html  200

# These extra rules are required because HTTPS is not forced
# http://postgwas.opentargets.io/* https://genetics.opentargets.io/:splat 301!

# Optional: Redirect default Netlify subdomain to primary domain
# https://confident-turing-87cdc5.netlify.com/* https://genetics.opentargets.io/:splat 301!
# http://confident-turing-87cdc5.netlify.com/* https://genetics.opentargets.io/:splat 301!
https://confident-turing-87cdc5.netlify.com/* https://postgwas.opentargets.io/:splat 301!
http://confident-turing-87cdc5.netlify.com/* https://postgwas.opentargets.io/:splat 301!
